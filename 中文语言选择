这个软件没有中文选项，为了添加中文语言这个选项，我全程是问ai怎么在这个软件中添加中文语言的选项，并且进行操作的。
如果有你想要中文语言的选项，可以下载完这个软件后，按照我的方法发操作。
下载完软件后，请双击打开ACBR这个漫画阅读软件文件夹，然后找到并打开其中名为 resources 的文件夹，里面极大概率有一个名为 app.asar 的文件。
把这个名为 app.asar 的文件复制上传给ai。我用的豆包，流程是在豆包对话框里点开语言，改成工作任务 云电脑，推理用 2.1 Turbo这个模式，你要充钱了可以选pro
然后复制贴贴以下8-18的指令发给ai，等着app.asar被ai修改完毕，接着再下载后ai修改的app.asar文件，名字也必须是app.asar，复制贴贴到这个app.asar 原来的位置，覆盖掉，重新打开，在设置里就会有中文语言的选项。 
以下是要复制的指令，记得app.asar 文件先上传给ai。

按照以下步骤说的，帮我把这个纯英文没中文的软件添加中文语言的选项，我已经把要进行修改的文件 app.asar 这个给你了：
检查 resources 文件夹（核心关键）
请双击打开 resources 文件夹。里面极大概率有一个名为 app.asar 的文件。这是软件真正的源代码和界面文本所在的位置。 如果你想“自己修改添加中文”，这就是你需要动手的地方。但普通用户无法直接修改，需要以下步骤：
高级修改步骤（需要一定的技术基础）：
安装 Node.js 环境。
在命令行安装 asar 工具：npm install -g asar。
解包文件：在 resources 文件夹下打开命令行，运行 asar extract app.asar app。
进入 app 文件夹，用文本编辑器（如 VSCode）全局搜索英文文本（例如搜索 "Open File" 或 "Favorites"）。
找到存放文本的 .json 或 .js 文件。你可以制作一个 zh-CN.json 翻译文件（如果代码结构支持），或者直接将代码里的英文硬替换成中文。
修改完成后，重新打包：asar pack app app.asar。
重启软件。
